class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        newnum = []

        for x in nums:
            newnum.append(x)

        for x in nums:
            newnum.append(x)
        
        return newnum